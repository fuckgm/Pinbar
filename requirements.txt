# ============================================================================
# Pinbar策略回测系统 - 趋势跟踪版依赖清单
# ============================================================================

# 核心交易框架
backtrader>=1.9.76.123
pandas>=1.5.0
numpy>=1.24.0
python-binance>=1.0.19

# 技术分析核心
ta-lib>=0.6.4

# 机器学习和统计分析 (新增 - ML趋势识别优化)
scikit-learn>=1.3.0
joblib>=1.3.0
statsmodels>=0.14.0

# 数值计算和优化
scipy>=1.10.0
scikit-optimize>=0.9.0  # 贝叶斯优化
numba>=0.58.0  # 数值计算加速

# 可视化和报告
plotly>=5.15.0
matplotlib>=3.7.0  # 新增 - 基础绘图
seaborn>=0.12.0  # 新增 - 统计可视化
jinja2>=3.1.0

# 交互式命令行
inquirer>=3.1.0

# 数据处理和导出
openpyxl>=3.1.0  # Excel导出
xlsxwriter>=3.1.0  # 新增 - Excel格式化写入

# 日期时间处理增强
pytz>=2023.3  # 新增 - 时区处理

# 网络请求 (数据下载)
requests>=2.31.0  # 新增 - HTTP请求
aiohttp>=3.8.0  # 新增 - 异步HTTP (可选)

# 配置文件处理
pyyaml>=6.0  # 新增 - YAML配置文件支持
configparser  # 内置 - INI配置文件

# 数据验证
pydantic>=2.0.0  # 新增 - 数据验证和设置管理

# 日志记录增强
loguru>=0.7.0  # 新增 - 更好的日志记录

# 并发处理
concurrent.futures  # 内置 - 多线程
asyncio  # 内置 - 异步处理

# 进度条
tqdm>=4.65.0  # 新增 - 进度条显示

# 缓存
cachetools>=5.3.0  # 新增 - 缓存工具

# 内存分析 (开发调试用，可选)
memory-profiler>=0.61.0  # 可选 - 内存使用分析
psutil>=5.9.0  # 新增 - 系统资源监控

# ============================================================================
# 注意事项:
# 1. Python 3.8+ 推荐使用 Python 3.9-3.11
# 2. TA-Lib 需要单独安装 C 库依赖
# 3. 机器学习功能需要 scikit-learn 和 joblib
# 4. 某些功能为可选依赖，可根据需要安装
# ============================================================================